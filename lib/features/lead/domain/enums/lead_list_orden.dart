// lib/features/lead/domain/enums/lead_list_orden.dart

enum LeadListOrden {
  ultimaInteraccion('Última interacción'),
  nombreAZ('Nombre (A-Z)');

  final String label;
  const LeadListOrden(this.label);
}
